---
alwaysApply: true
---
# Markdown 输出规范

- 标题层级：最多用到 `##` 和 `###`，避免 `#` 充斥页面。
- 代码块：
  - 引用现有代码/文档路径时使用反引号；
  - 新提案/示例使用标准 fenced code blocks（指定语言）。
- 表格：用于对比“内涵/区别/适用性”等高密度信息。
- 列表：优先短句，动词开头，便于扫描。
- 文件/目录/函数/类名：始终使用反引号包裹。

